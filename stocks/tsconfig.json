{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.test.json" }
  ],
  // The same alias as tsconfig.app.json. Nothing is compiled from this file;
  // it is here because Bun reads only tsconfig.json when running the tests.
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
