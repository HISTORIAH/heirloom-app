/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STOCK_PLAN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  235, 253, 221, 198, 146, 187, 60, 157,
]);

export function getStockPlanDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STOCK_PLAN_DISCRIMINATOR);
}

export type StockPlan = {
  discriminator: ReadonlyUint8Array;
  version: number;
  owner: Address;
  /**
   * Where assets go when the plan fires. The owner's own recovery wallet in
   * backup mode, the heir in vault mode. Fixed on-chain, so a recovery can
   * never be redirected by whoever submits it.
   */
  destination: Address;
  /** `PLAN_MODE_BACKUP` or `PLAN_MODE_VAULT`. */
  mode: number;
  checkinIntervalSecs: bigint;
  gracePeriodSecs: bigint;
  lastCheckinTs: bigint;
  createdAt: bigint;
  /** Seconds a guardian adds to the deadline with a single defer. */
  pauseDurationSecs: bigint;
  /** Absolute deadline extension from a guardian defer. 0 when not deferred. */
  pausedUntil: bigint;
  /** Hot wallet permitted to check in but nothing else. */
  checkinSigner: Option<Address>;
  /** Third party permitted to defer the deadline but nothing else. */
  guardian: Option<Address>;
  /** Number of live `CoveredAsset` records under this plan. */
  coveredAssets: number;
  bump: number;
};

export type StockPlanArgs = {
  version: number;
  owner: Address;
  /**
   * Where assets go when the plan fires. The owner's own recovery wallet in
   * backup mode, the heir in vault mode. Fixed on-chain, so a recovery can
   * never be redirected by whoever submits it.
   */
  destination: Address;
  /** `PLAN_MODE_BACKUP` or `PLAN_MODE_VAULT`. */
  mode: number;
  checkinIntervalSecs: number | bigint;
  gracePeriodSecs: number | bigint;
  lastCheckinTs: number | bigint;
  createdAt: number | bigint;
  /** Seconds a guardian adds to the deadline with a single defer. */
  pauseDurationSecs: number | bigint;
  /** Absolute deadline extension from a guardian defer. 0 when not deferred. */
  pausedUntil: number | bigint;
  /** Hot wallet permitted to check in but nothing else. */
  checkinSigner: OptionOrNullable<Address>;
  /** Third party permitted to defer the deadline but nothing else. */
  guardian: OptionOrNullable<Address>;
  /** Number of live `CoveredAsset` records under this plan. */
  coveredAssets: number;
  bump: number;
};

/** Gets the encoder for {@link StockPlanArgs} account data. */
export function getStockPlanEncoder(): Encoder<StockPlanArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["owner", getAddressEncoder()],
      ["destination", getAddressEncoder()],
      ["mode", getU8Encoder()],
      ["checkinIntervalSecs", getI64Encoder()],
      ["gracePeriodSecs", getI64Encoder()],
      ["lastCheckinTs", getI64Encoder()],
      ["createdAt", getI64Encoder()],
      ["pauseDurationSecs", getI64Encoder()],
      ["pausedUntil", getI64Encoder()],
      ["checkinSigner", getOptionEncoder(getAddressEncoder())],
      ["guardian", getOptionEncoder(getAddressEncoder())],
      ["coveredAssets", getU16Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STOCK_PLAN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link StockPlan} account data. */
export function getStockPlanDecoder(): Decoder<StockPlan> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["owner", getAddressDecoder()],
    ["destination", getAddressDecoder()],
    ["mode", getU8Decoder()],
    ["checkinIntervalSecs", getI64Decoder()],
    ["gracePeriodSecs", getI64Decoder()],
    ["lastCheckinTs", getI64Decoder()],
    ["createdAt", getI64Decoder()],
    ["pauseDurationSecs", getI64Decoder()],
    ["pausedUntil", getI64Decoder()],
    ["checkinSigner", getOptionDecoder(getAddressDecoder())],
    ["guardian", getOptionDecoder(getAddressDecoder())],
    ["coveredAssets", getU16Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link StockPlan} account data. */
export function getStockPlanCodec(): Codec<StockPlanArgs, StockPlan> {
  return combineCodec(getStockPlanEncoder(), getStockPlanDecoder());
}

export function decodeStockPlan<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<StockPlan, TAddress>;
export function decodeStockPlan<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<StockPlan, TAddress>;
export function decodeStockPlan<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<StockPlan, TAddress> | MaybeAccount<StockPlan, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getStockPlanDecoder());
}

export async function fetchStockPlan<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<StockPlan, TAddress>> {
  const maybeAccount = await fetchMaybeStockPlan(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStockPlan<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<StockPlan, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStockPlan(maybeAccount);
}

export async function fetchAllStockPlan(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<StockPlan>[]> {
  const maybeAccounts = await fetchAllMaybeStockPlan(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStockPlan(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<StockPlan>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStockPlan(maybeAccount));
}
