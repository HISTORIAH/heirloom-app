/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ISSUER_REGISTRY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  252, 217, 20, 87, 39, 96, 228, 46,
]);

export function getIssuerRegistryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ISSUER_REGISTRY_DISCRIMINATOR);
}

export type IssuerRegistry = {
  discriminator: ReadonlyUint8Array;
  version: number;
  /** The mint authority every mint from this issuer carries. */
  mintAuthority: Address;
  /** Short display label, zero-padded (e.g. `xstocks`). */
  label: ReadonlyUint8Array;
  /**
   * Higher means more issuer control over holder funds. Surfaced in the UI,
   * not enforced on-chain.
   */
  riskTier: number;
  enabled: boolean;
  bump: number;
};

export type IssuerRegistryArgs = {
  version: number;
  /** The mint authority every mint from this issuer carries. */
  mintAuthority: Address;
  /** Short display label, zero-padded (e.g. `xstocks`). */
  label: ReadonlyUint8Array;
  /**
   * Higher means more issuer control over holder funds. Surfaced in the UI,
   * not enforced on-chain.
   */
  riskTier: number;
  enabled: boolean;
  bump: number;
};

/** Gets the encoder for {@link IssuerRegistryArgs} account data. */
export function getIssuerRegistryEncoder(): FixedSizeEncoder<IssuerRegistryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["mintAuthority", getAddressEncoder()],
      ["label", fixEncoderSize(getBytesEncoder(), 16)],
      ["riskTier", getU8Encoder()],
      ["enabled", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ISSUER_REGISTRY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link IssuerRegistry} account data. */
export function getIssuerRegistryDecoder(): FixedSizeDecoder<IssuerRegistry> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["mintAuthority", getAddressDecoder()],
    ["label", fixDecoderSize(getBytesDecoder(), 16)],
    ["riskTier", getU8Decoder()],
    ["enabled", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link IssuerRegistry} account data. */
export function getIssuerRegistryCodec(): FixedSizeCodec<IssuerRegistryArgs, IssuerRegistry> {
  return combineCodec(getIssuerRegistryEncoder(), getIssuerRegistryDecoder());
}

export function decodeIssuerRegistry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<IssuerRegistry, TAddress>;
export function decodeIssuerRegistry<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<IssuerRegistry, TAddress>;
export function decodeIssuerRegistry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<IssuerRegistry, TAddress> | MaybeAccount<IssuerRegistry, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getIssuerRegistryDecoder());
}

export async function fetchIssuerRegistry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<IssuerRegistry, TAddress>> {
  const maybeAccount = await fetchMaybeIssuerRegistry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeIssuerRegistry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<IssuerRegistry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeIssuerRegistry(maybeAccount);
}

export async function fetchAllIssuerRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<IssuerRegistry>[]> {
  const maybeAccounts = await fetchAllMaybeIssuerRegistry(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeIssuerRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<IssuerRegistry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeIssuerRegistry(maybeAccount));
}

export function getIssuerRegistrySize(): number {
  return 60;
}
