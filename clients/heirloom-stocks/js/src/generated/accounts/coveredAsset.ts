/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const COVERED_ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  208, 221, 108, 84, 47, 77, 169, 1,
]);

export function getCoveredAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(COVERED_ASSET_DISCRIMINATOR);
}

export type CoveredAsset = {
  discriminator: ReadonlyUint8Array;
  version: number;
  mint: Address;
  /**
   * The token account this record covers: the owner's own account in backup
   * mode, the plan-owned vault account in vault mode.
   */
  sourceTokenAccount: Address;
  /**
   * The mint's raw decimals, cached for `transfer_checked`.
   *
   * This is the raw value, not a display scale. Issuers pay dividends and
   * apply splits by moving the `ScaledUiAmount` multiplier, which never
   * changes raw balances, so all transfer math stays in raw units.
   */
  mintDecimals: number;
  /**
   * Share of the balance that moves on recovery, in basis points.
   *
   * Always `BPS_DENOMINATOR` in vault mode, where the owner sets how much is
   * covered by how much they deposit.
   */
  allocationBps: number;
  /**
   * Whether the mint had a permanent delegate when it was covered. Re-checked
   * on recovery so an issuer silently gaining clawback power is detectable.
   */
  hadPermanentDelegate: boolean;
  coveredAt: bigint;
  bump: number;
};

export type CoveredAssetArgs = {
  version: number;
  mint: Address;
  /**
   * The token account this record covers: the owner's own account in backup
   * mode, the plan-owned vault account in vault mode.
   */
  sourceTokenAccount: Address;
  /**
   * The mint's raw decimals, cached for `transfer_checked`.
   *
   * This is the raw value, not a display scale. Issuers pay dividends and
   * apply splits by moving the `ScaledUiAmount` multiplier, which never
   * changes raw balances, so all transfer math stays in raw units.
   */
  mintDecimals: number;
  /**
   * Share of the balance that moves on recovery, in basis points.
   *
   * Always `BPS_DENOMINATOR` in vault mode, where the owner sets how much is
   * covered by how much they deposit.
   */
  allocationBps: number;
  /**
   * Whether the mint had a permanent delegate when it was covered. Re-checked
   * on recovery so an issuer silently gaining clawback power is detectable.
   */
  hadPermanentDelegate: boolean;
  coveredAt: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link CoveredAssetArgs} account data. */
export function getCoveredAssetEncoder(): FixedSizeEncoder<CoveredAssetArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU8Encoder()],
      ["mint", getAddressEncoder()],
      ["sourceTokenAccount", getAddressEncoder()],
      ["mintDecimals", getU8Encoder()],
      ["allocationBps", getU16Encoder()],
      ["hadPermanentDelegate", getBooleanEncoder()],
      ["coveredAt", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: COVERED_ASSET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CoveredAsset} account data. */
export function getCoveredAssetDecoder(): FixedSizeDecoder<CoveredAsset> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU8Decoder()],
    ["mint", getAddressDecoder()],
    ["sourceTokenAccount", getAddressDecoder()],
    ["mintDecimals", getU8Decoder()],
    ["allocationBps", getU16Decoder()],
    ["hadPermanentDelegate", getBooleanDecoder()],
    ["coveredAt", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link CoveredAsset} account data. */
export function getCoveredAssetCodec(): FixedSizeCodec<CoveredAssetArgs, CoveredAsset> {
  return combineCodec(getCoveredAssetEncoder(), getCoveredAssetDecoder());
}

export function decodeCoveredAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CoveredAsset, TAddress>;
export function decodeCoveredAsset<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CoveredAsset, TAddress>;
export function decodeCoveredAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CoveredAsset, TAddress> | MaybeAccount<CoveredAsset, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getCoveredAssetDecoder());
}

export async function fetchCoveredAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CoveredAsset, TAddress>> {
  const maybeAccount = await fetchMaybeCoveredAsset(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCoveredAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CoveredAsset, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCoveredAsset(maybeAccount);
}

export async function fetchAllCoveredAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CoveredAsset>[]> {
  const maybeAccounts = await fetchAllMaybeCoveredAsset(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCoveredAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CoveredAsset>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCoveredAsset(maybeAccount));
}

export function getCoveredAssetSize(): number {
  return 86;
}
