/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_PLAN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  45, 137, 184, 220, 162, 253, 161, 8,
]);

export function getClosePlanDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_PLAN_DISCRIMINATOR);
}

export type ClosePlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClosePlanInstructionData = { discriminator: ReadonlyUint8Array };

export type ClosePlanInstructionDataArgs = {};

export function getClosePlanInstructionDataEncoder(): FixedSizeEncoder<ClosePlanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_PLAN_DISCRIMINATOR }),
  );
}

export function getClosePlanInstructionDataDecoder(): FixedSizeDecoder<ClosePlanInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getClosePlanInstructionDataCodec(): FixedSizeCodec<
  ClosePlanInstructionDataArgs,
  ClosePlanInstructionData
> {
  return combineCodec(getClosePlanInstructionDataEncoder(), getClosePlanInstructionDataDecoder());
}

export type ClosePlanInput<
  TAccountOwner extends string = string,
  TAccountPlan extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  plan: Address<TAccountPlan>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClosePlanInstruction<
  TAccountOwner extends string,
  TAccountPlan extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: ClosePlanInput<TAccountOwner, TAccountPlan, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): ClosePlanInstruction<TProgramAddress, TAccountOwner, TAccountPlan, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClosePlanInstructionDataEncoder().encode({}),
    programAddress,
  } as ClosePlanInstruction<TProgramAddress, TAccountOwner, TAccountPlan, TAccountSystemProgram>);
}

export type ParsedClosePlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    plan: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: ClosePlanInstructionData;
};

export function parseClosePlanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClosePlanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), plan: getNextAccount(), systemProgram: getNextAccount() },
    data: getClosePlanInstructionDataDecoder().decode(instruction.data),
  };
}
