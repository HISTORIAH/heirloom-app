/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_PLAN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  119, 112, 58, 60, 76, 205, 1, 100,
]);

export function getUpdatePlanDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_PLAN_DISCRIMINATOR);
}

export type UpdatePlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountNewDestination extends string | AccountMeta<string> = string,
  TAccountNewCheckinSigner extends string | AccountMeta<string> = string,
  TAccountNewGuardian extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountNewDestination extends string
        ? ReadonlyAccount<TAccountNewDestination>
        : TAccountNewDestination,
      TAccountNewCheckinSigner extends string
        ? ReadonlyAccount<TAccountNewCheckinSigner>
        : TAccountNewCheckinSigner,
      TAccountNewGuardian extends string
        ? ReadonlyAccount<TAccountNewGuardian>
        : TAccountNewGuardian,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePlanInstructionData = {
  discriminator: ReadonlyUint8Array;
  checkinIntervalSecs: Option<bigint>;
  gracePeriodSecs: Option<bigint>;
  pauseDurationSecs: Option<bigint>;
  clearCheckinSigner: boolean;
  clearGuardian: boolean;
};

export type UpdatePlanInstructionDataArgs = {
  checkinIntervalSecs: OptionOrNullable<number | bigint>;
  gracePeriodSecs: OptionOrNullable<number | bigint>;
  pauseDurationSecs: OptionOrNullable<number | bigint>;
  clearCheckinSigner: boolean;
  clearGuardian: boolean;
};

export function getUpdatePlanInstructionDataEncoder(): Encoder<UpdatePlanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["checkinIntervalSecs", getOptionEncoder(getI64Encoder())],
      ["gracePeriodSecs", getOptionEncoder(getI64Encoder())],
      ["pauseDurationSecs", getOptionEncoder(getI64Encoder())],
      ["clearCheckinSigner", getBooleanEncoder()],
      ["clearGuardian", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PLAN_DISCRIMINATOR }),
  );
}

export function getUpdatePlanInstructionDataDecoder(): Decoder<UpdatePlanInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["checkinIntervalSecs", getOptionDecoder(getI64Decoder())],
    ["gracePeriodSecs", getOptionDecoder(getI64Decoder())],
    ["pauseDurationSecs", getOptionDecoder(getI64Decoder())],
    ["clearCheckinSigner", getBooleanDecoder()],
    ["clearGuardian", getBooleanDecoder()],
  ]);
}

export function getUpdatePlanInstructionDataCodec(): Codec<
  UpdatePlanInstructionDataArgs,
  UpdatePlanInstructionData
> {
  return combineCodec(getUpdatePlanInstructionDataEncoder(), getUpdatePlanInstructionDataDecoder());
}

export type UpdatePlanInput<
  TAccountOwner extends string = string,
  TAccountNewDestination extends string = string,
  TAccountNewCheckinSigner extends string = string,
  TAccountNewGuardian extends string = string,
  TAccountPlan extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** CHECK: new destination pubkey, when one is supplied. */
  newDestination?: Address<TAccountNewDestination>;
  /** CHECK: new hot check-in wallet, when one is supplied. */
  newCheckinSigner?: Address<TAccountNewCheckinSigner>;
  /** CHECK: new guardian, when one is supplied. */
  newGuardian?: Address<TAccountNewGuardian>;
  plan: Address<TAccountPlan>;
  systemProgram?: Address<TAccountSystemProgram>;
  checkinIntervalSecs: UpdatePlanInstructionDataArgs["checkinIntervalSecs"];
  gracePeriodSecs: UpdatePlanInstructionDataArgs["gracePeriodSecs"];
  pauseDurationSecs: UpdatePlanInstructionDataArgs["pauseDurationSecs"];
  clearCheckinSigner: UpdatePlanInstructionDataArgs["clearCheckinSigner"];
  clearGuardian: UpdatePlanInstructionDataArgs["clearGuardian"];
};

export function getUpdatePlanInstruction<
  TAccountOwner extends string,
  TAccountNewDestination extends string,
  TAccountNewCheckinSigner extends string,
  TAccountNewGuardian extends string,
  TAccountPlan extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: UpdatePlanInput<
    TAccountOwner,
    TAccountNewDestination,
    TAccountNewCheckinSigner,
    TAccountNewGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdatePlanInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountNewDestination,
  TAccountNewCheckinSigner,
  TAccountNewGuardian,
  TAccountPlan,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    newDestination: { value: input.newDestination ?? null, isWritable: false },
    newCheckinSigner: { value: input.newCheckinSigner ?? null, isWritable: false },
    newGuardian: { value: input.newGuardian ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("newDestination", accounts.newDestination),
      getAccountMeta("newCheckinSigner", accounts.newCheckinSigner),
      getAccountMeta("newGuardian", accounts.newGuardian),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdatePlanInstructionDataEncoder().encode(args as UpdatePlanInstructionDataArgs),
    programAddress,
  } as UpdatePlanInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountNewDestination,
    TAccountNewCheckinSigner,
    TAccountNewGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >);
}

export type ParsedUpdatePlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    /** CHECK: new destination pubkey, when one is supplied. */
    newDestination?: TAccountMetas[1] | undefined;
    /** CHECK: new hot check-in wallet, when one is supplied. */
    newCheckinSigner?: TAccountMetas[2] | undefined;
    /** CHECK: new guardian, when one is supplied. */
    newGuardian?: TAccountMetas[3] | undefined;
    plan: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: UpdatePlanInstructionData;
};

export function parseUpdatePlanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdatePlanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === HEIRLOOM_STOCKS_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      newDestination: getNextOptionalAccount(),
      newCheckinSigner: getNextOptionalAccount(),
      newGuardian: getNextOptionalAccount(),
      plan: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdatePlanInstructionDataDecoder().decode(instruction.data),
  };
}
