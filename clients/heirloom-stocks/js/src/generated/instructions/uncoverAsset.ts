/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const UNCOVER_ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  17, 106, 41, 79, 113, 143, 249, 70,
]);

export function getUncoverAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UNCOVER_ASSET_DISCRIMINATOR);
}

export type UncoverAssetInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountCoveredAsset extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountCoveredAsset extends string
        ? WritableAccount<TAccountCoveredAsset>
        : TAccountCoveredAsset,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UncoverAssetInstructionData = { discriminator: ReadonlyUint8Array };

export type UncoverAssetInstructionDataArgs = {};

export function getUncoverAssetInstructionDataEncoder(): FixedSizeEncoder<UncoverAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNCOVER_ASSET_DISCRIMINATOR }),
  );
}

export function getUncoverAssetInstructionDataDecoder(): FixedSizeDecoder<UncoverAssetInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getUncoverAssetInstructionDataCodec(): FixedSizeCodec<
  UncoverAssetInstructionDataArgs,
  UncoverAssetInstructionData
> {
  return combineCodec(
    getUncoverAssetInstructionDataEncoder(),
    getUncoverAssetInstructionDataDecoder(),
  );
}

export type UncoverAssetInput<
  TAccountOwner extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** The account the marker names, or `None` once the owner has closed it. */
  ownerTokenAccount?: Address<TAccountOwnerTokenAccount>;
  plan: Address<TAccountPlan>;
  coveredAsset: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getUncoverAssetInstruction<
  TAccountOwner extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountPlan extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: UncoverAssetInput<
    TAccountOwner,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UncoverAssetInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountOwnerTokenAccount,
  TAccountPlan,
  TAccountCoveredAsset,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUncoverAssetInstructionDataEncoder().encode({}),
    programAddress,
  } as UncoverAssetInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedUncoverAssetInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    /** The account the marker names, or `None` once the owner has closed it. */
    ownerTokenAccount?: TAccountMetas[1] | undefined;
    plan: TAccountMetas[2];
    coveredAsset: TAccountMetas[3];
    /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
    tokenProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: UncoverAssetInstructionData;
};

export function parseUncoverAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUncoverAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === HEIRLOOM_STOCKS_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      ownerTokenAccount: getNextOptionalAccount(),
      plan: getNextAccount(),
      coveredAsset: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUncoverAssetInstructionDataDecoder().decode(instruction.data),
  };
}
