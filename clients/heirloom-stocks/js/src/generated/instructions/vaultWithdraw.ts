/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const VAULT_WITHDRAW_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  98, 28, 187, 98, 87, 69, 46, 64,
]);

export function getVaultWithdrawDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_WITHDRAW_DISCRIMINATOR);
}

export type VaultWithdrawInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> =
    "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q",
  TAccountTreasuryTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountCoveredAsset extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountTreasury extends string ? WritableAccount<TAccountTreasury> : TAccountTreasury,
      TAccountTreasuryTokenAccount extends string
        ? WritableAccount<TAccountTreasuryTokenAccount>
        : TAccountTreasuryTokenAccount,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountCoveredAsset extends string
        ? WritableAccount<TAccountCoveredAsset>
        : TAccountCoveredAsset,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type VaultWithdrawInstructionData = { discriminator: ReadonlyUint8Array; amount: bigint };

export type VaultWithdrawInstructionDataArgs = { amount: number | bigint };

export function getVaultWithdrawInstructionDataEncoder(): FixedSizeEncoder<VaultWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VAULT_WITHDRAW_DISCRIMINATOR }),
  );
}

export function getVaultWithdrawInstructionDataDecoder(): FixedSizeDecoder<VaultWithdrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getVaultWithdrawInstructionDataCodec(): FixedSizeCodec<
  VaultWithdrawInstructionDataArgs,
  VaultWithdrawInstructionData
> {
  return combineCodec(
    getVaultWithdrawInstructionDataEncoder(),
    getVaultWithdrawInstructionDataDecoder(),
  );
}

export type VaultWithdrawInput<
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountTreasury extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  /** CHECK: owner ATA, created idempotently in case they closed it. */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /** CHECK: treasury address. */
  treasury?: Address<TAccountTreasury>;
  /** CHECK: treasury ATA, created idempotently if needed. */
  treasuryTokenAccount: Address<TAccountTreasuryTokenAccount>;
  plan: Address<TAccountPlan>;
  coveredAsset: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: VaultWithdrawInstructionDataArgs["amount"];
};

export function getVaultWithdrawInstruction<
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountVaultTokenAccount extends string,
  TAccountTreasury extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountPlan extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: VaultWithdrawInput<
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTreasury,
    TAccountTreasuryTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): VaultWithdrawInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountMint,
  TAccountOwnerTokenAccount,
  TAccountVaultTokenAccount,
  TAccountTreasury,
  TAccountTreasuryTokenAccount,
  TAccountPlan,
  TAccountCoveredAsset,
  TAccountTokenProgram,
  TAccountAssociatedTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    vaultTokenAccount: { value: input.vaultTokenAccount ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    treasuryTokenAccount: { value: input.treasuryTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.treasury.value) {
    accounts.treasury.value =
      "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q" as Address<"tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("treasuryTokenAccount", accounts.treasuryTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getVaultWithdrawInstructionDataEncoder().encode(args as VaultWithdrawInstructionDataArgs),
    programAddress,
  } as VaultWithdrawInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTreasury,
    TAccountTreasuryTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedVaultWithdrawInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    mint: TAccountMetas[1];
    /** CHECK: owner ATA, created idempotently in case they closed it. */
    ownerTokenAccount: TAccountMetas[2];
    vaultTokenAccount: TAccountMetas[3];
    /** CHECK: treasury address. */
    treasury: TAccountMetas[4];
    /** CHECK: treasury ATA, created idempotently if needed. */
    treasuryTokenAccount: TAccountMetas[5];
    plan: TAccountMetas[6];
    coveredAsset: TAccountMetas[7];
    /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
    tokenProgram: TAccountMetas[8];
    associatedTokenProgram: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
  };
  data: VaultWithdrawInstructionData;
};

export function parseVaultWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 11,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      mint: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      treasury: getNextAccount(),
      treasuryTokenAccount: getNextAccount(),
      plan: getNextAccount(),
      coveredAsset: getNextAccount(),
      tokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getVaultWithdrawInstructionDataDecoder().decode(instruction.data),
  };
}
