/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findIssuerPda } from "../pdas";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ISSUER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  9, 100, 234, 30, 84, 43, 30, 29,
]);

export function getUpdateIssuerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ISSUER_DISCRIMINATOR);
}

export type UpdateIssuerInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> =
    "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q",
  TAccountMintAuthority extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMintAuthority extends string
        ? ReadonlyAccount<TAccountMintAuthority>
        : TAccountMintAuthority,
      TAccountIssuer extends string ? WritableAccount<TAccountIssuer> : TAccountIssuer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateIssuerInstructionData = {
  discriminator: ReadonlyUint8Array;
  enabled: Option<boolean>;
  riskTier: Option<number>;
};

export type UpdateIssuerInstructionDataArgs = {
  enabled: OptionOrNullable<boolean>;
  riskTier: OptionOrNullable<number>;
};

export function getUpdateIssuerInstructionDataEncoder(): Encoder<UpdateIssuerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["enabled", getOptionEncoder(getBooleanEncoder())],
      ["riskTier", getOptionEncoder(getU8Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ISSUER_DISCRIMINATOR }),
  );
}

export function getUpdateIssuerInstructionDataDecoder(): Decoder<UpdateIssuerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["enabled", getOptionDecoder(getBooleanDecoder())],
    ["riskTier", getOptionDecoder(getU8Decoder())],
  ]);
}

export function getUpdateIssuerInstructionDataCodec(): Codec<
  UpdateIssuerInstructionDataArgs,
  UpdateIssuerInstructionData
> {
  return combineCodec(
    getUpdateIssuerInstructionDataEncoder(),
    getUpdateIssuerInstructionDataDecoder(),
  );
}

export type UpdateIssuerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMintAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin?: TransactionSigner<TAccountAdmin>;
  /** CHECK: the issuer's mint authority; only its address is used, as a seed. */
  mintAuthority: Address<TAccountMintAuthority>;
  issuer?: Address<TAccountIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  enabled: UpdateIssuerInstructionDataArgs["enabled"];
  riskTier: UpdateIssuerInstructionDataArgs["riskTier"];
};

export async function getUpdateIssuerInstructionAsync<
  TAccountAdmin extends string,
  TAccountMintAuthority extends string,
  TAccountIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: UpdateIssuerAsyncInput<
    TAccountAdmin,
    TAccountMintAuthority,
    TAccountIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMintAuthority,
    TAccountIssuer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    mintAuthority: { value: input.mintAuthority ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.admin.value) {
    accounts.admin.value =
      "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q" as Address<"tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q">;
  }
  if (!accounts.issuer.value) {
    accounts.issuer.value = await findIssuerPda({
      mintAuthority: getAddressFromResolvedInstructionAccount(
        "mintAuthority",
        accounts.mintAuthority.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("mintAuthority", accounts.mintAuthority),
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateIssuerInstructionDataEncoder().encode(args as UpdateIssuerInstructionDataArgs),
    programAddress,
  } as UpdateIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMintAuthority,
    TAccountIssuer,
    TAccountSystemProgram
  >);
}

export type UpdateIssuerInput<
  TAccountAdmin extends string = string,
  TAccountMintAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin?: TransactionSigner<TAccountAdmin>;
  /** CHECK: the issuer's mint authority; only its address is used, as a seed. */
  mintAuthority: Address<TAccountMintAuthority>;
  issuer: Address<TAccountIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  enabled: UpdateIssuerInstructionDataArgs["enabled"];
  riskTier: UpdateIssuerInstructionDataArgs["riskTier"];
};

export function getUpdateIssuerInstruction<
  TAccountAdmin extends string,
  TAccountMintAuthority extends string,
  TAccountIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: UpdateIssuerInput<
    TAccountAdmin,
    TAccountMintAuthority,
    TAccountIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateIssuerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMintAuthority,
  TAccountIssuer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    mintAuthority: { value: input.mintAuthority ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.admin.value) {
    accounts.admin.value =
      "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q" as Address<"tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("mintAuthority", accounts.mintAuthority),
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateIssuerInstructionDataEncoder().encode(args as UpdateIssuerInstructionDataArgs),
    programAddress,
  } as UpdateIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMintAuthority,
    TAccountIssuer,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateIssuerInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    /** CHECK: the issuer's mint authority; only its address is used, as a seed. */
    mintAuthority: TAccountMetas[1];
    issuer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: UpdateIssuerInstructionData;
};

export function parseUpdateIssuerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateIssuerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      mintAuthority: getNextAccount(),
      issuer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateIssuerInstructionDataDecoder().decode(instruction.data),
  };
}
