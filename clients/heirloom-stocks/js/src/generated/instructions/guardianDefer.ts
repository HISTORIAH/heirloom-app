/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const GUARDIAN_DEFER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  61, 73, 203, 161, 131, 27, 54, 51,
]);

export function getGuardianDeferDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GUARDIAN_DEFER_DISCRIMINATOR);
}

export type GuardianDeferInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountGuardian extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGuardian extends string
        ? WritableSignerAccount<TAccountGuardian> & AccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GuardianDeferInstructionData = { discriminator: ReadonlyUint8Array };

export type GuardianDeferInstructionDataArgs = {};

export function getGuardianDeferInstructionDataEncoder(): FixedSizeEncoder<GuardianDeferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GUARDIAN_DEFER_DISCRIMINATOR }),
  );
}

export function getGuardianDeferInstructionDataDecoder(): FixedSizeDecoder<GuardianDeferInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getGuardianDeferInstructionDataCodec(): FixedSizeCodec<
  GuardianDeferInstructionDataArgs,
  GuardianDeferInstructionData
> {
  return combineCodec(
    getGuardianDeferInstructionDataEncoder(),
    getGuardianDeferInstructionDataDecoder(),
  );
}

export type GuardianDeferInput<
  TAccountGuardian extends string = string,
  TAccountPlan extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  guardian: TransactionSigner<TAccountGuardian>;
  plan: Address<TAccountPlan>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getGuardianDeferInstruction<
  TAccountGuardian extends string,
  TAccountPlan extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: GuardianDeferInput<TAccountGuardian, TAccountPlan, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): GuardianDeferInstruction<
  TProgramAddress,
  TAccountGuardian,
  TAccountPlan,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    guardian: { value: input.guardian ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("guardian", accounts.guardian),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getGuardianDeferInstructionDataEncoder().encode({}),
    programAddress,
  } as GuardianDeferInstruction<
    TProgramAddress,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >);
}

export type ParsedGuardianDeferInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    guardian: TAccountMetas[0];
    plan: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: GuardianDeferInstructionData;
};

export function parseGuardianDeferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGuardianDeferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      guardian: getNextAccount(),
      plan: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getGuardianDeferInstructionDataDecoder().decode(instruction.data),
  };
}
