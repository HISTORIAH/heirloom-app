/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findCoveredAssetPda } from "../pdas";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const COVER_ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  104, 152, 113, 76, 39, 115, 249, 63,
]);

export function getCoverAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(COVER_ASSET_DISCRIMINATOR);
}

export type CoverAssetInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountCoveredAsset extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountIssuer extends string ? ReadonlyAccount<TAccountIssuer> : TAccountIssuer,
      TAccountCoveredAsset extends string
        ? WritableAccount<TAccountCoveredAsset>
        : TAccountCoveredAsset,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CoverAssetInstructionData = {
  discriminator: ReadonlyUint8Array;
  allocationBps: number;
};

export type CoverAssetInstructionDataArgs = { allocationBps: number };

export function getCoverAssetInstructionDataEncoder(): FixedSizeEncoder<CoverAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allocationBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: COVER_ASSET_DISCRIMINATOR }),
  );
}

export function getCoverAssetInstructionDataDecoder(): FixedSizeDecoder<CoverAssetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allocationBps", getU16Decoder()],
  ]);
}

export function getCoverAssetInstructionDataCodec(): FixedSizeCodec<
  CoverAssetInstructionDataArgs,
  CoverAssetInstructionData
> {
  return combineCodec(getCoverAssetInstructionDataEncoder(), getCoverAssetInstructionDataDecoder());
}

export type CoverAssetAsyncInput<
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountIssuer extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  /** The owner's own token account. Stays owned by the owner throughout. */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  plan: Address<TAccountPlan>;
  /**
   * Curation entry for the issuer that minted `mint`. Its stored authority is
   * checked against the mint's real authority in `validate`, so this cannot be
   * satisfied with an entry belonging to a different issuer.
   */
  issuer: Address<TAccountIssuer>;
  coveredAsset?: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  allocationBps: CoverAssetInstructionDataArgs["allocationBps"];
};

export async function getCoverAssetInstructionAsync<
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountPlan extends string,
  TAccountIssuer extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: CoverAssetAsyncInput<
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountIssuer,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CoverAssetInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountIssuer,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.coveredAsset.value) {
    accounts.coveredAsset.value = await findCoveredAssetPda({
      plan: getAddressFromResolvedInstructionAccount("plan", accounts.plan.value),
      mint: getAddressFromResolvedInstructionAccount("mint", accounts.mint.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCoverAssetInstructionDataEncoder().encode(args as CoverAssetInstructionDataArgs),
    programAddress,
  } as CoverAssetInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountIssuer,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type CoverAssetInput<
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountIssuer extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  /** The owner's own token account. Stays owned by the owner throughout. */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  plan: Address<TAccountPlan>;
  /**
   * Curation entry for the issuer that minted `mint`. Its stored authority is
   * checked against the mint's real authority in `validate`, so this cannot be
   * satisfied with an entry belonging to a different issuer.
   */
  issuer: Address<TAccountIssuer>;
  coveredAsset: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  allocationBps: CoverAssetInstructionDataArgs["allocationBps"];
};

export function getCoverAssetInstruction<
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountPlan extends string,
  TAccountIssuer extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: CoverAssetInput<
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountIssuer,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CoverAssetInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountMint,
  TAccountOwnerTokenAccount,
  TAccountPlan,
  TAccountIssuer,
  TAccountCoveredAsset,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCoverAssetInstructionDataEncoder().encode(args as CoverAssetInstructionDataArgs),
    programAddress,
  } as CoverAssetInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountPlan,
    TAccountIssuer,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCoverAssetInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    mint: TAccountMetas[1];
    /** The owner's own token account. Stays owned by the owner throughout. */
    ownerTokenAccount: TAccountMetas[2];
    plan: TAccountMetas[3];
    /**
     * Curation entry for the issuer that minted `mint`. Its stored authority is
     * checked against the mint's real authority in `validate`, so this cannot be
     * satisfied with an entry belonging to a different issuer.
     */
    issuer: TAccountMetas[4];
    coveredAsset: TAccountMetas[5];
    /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
    tokenProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: CoverAssetInstructionData;
};

export function parseCoverAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCoverAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 8,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      mint: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      plan: getNextAccount(),
      issuer: getNextAccount(),
      coveredAsset: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCoverAssetInstructionDataDecoder().decode(instruction.data),
  };
}
