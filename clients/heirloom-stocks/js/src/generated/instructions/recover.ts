/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const RECOVER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  108, 216, 38, 58, 109, 146, 116, 17,
]);

export function getRecoverDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RECOVER_DISCRIMINATOR);
}

export type RecoverInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountDestinationTokenAccount extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> =
    "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q",
  TAccountTreasuryTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountCoveredAsset extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDestination extends string
        ? WritableSignerAccount<TAccountDestination> & AccountSignerMeta<TAccountDestination>
        : TAccountDestination,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountDestinationTokenAccount extends string
        ? WritableAccount<TAccountDestinationTokenAccount>
        : TAccountDestinationTokenAccount,
      TAccountTreasury extends string ? WritableAccount<TAccountTreasury> : TAccountTreasury,
      TAccountTreasuryTokenAccount extends string
        ? WritableAccount<TAccountTreasuryTokenAccount>
        : TAccountTreasuryTokenAccount,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountCoveredAsset extends string
        ? WritableAccount<TAccountCoveredAsset>
        : TAccountCoveredAsset,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecoverInstructionData = { discriminator: ReadonlyUint8Array };

export type RecoverInstructionDataArgs = {};

export function getRecoverInstructionDataEncoder(): FixedSizeEncoder<RecoverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RECOVER_DISCRIMINATOR }),
  );
}

export function getRecoverInstructionDataDecoder(): FixedSizeDecoder<RecoverInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getRecoverInstructionDataCodec(): FixedSizeCodec<
  RecoverInstructionDataArgs,
  RecoverInstructionData
> {
  return combineCodec(getRecoverInstructionDataEncoder(), getRecoverInstructionDataDecoder());
}

export type RecoverInput<
  TAccountDestination extends string = string,
  TAccountMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountDestinationTokenAccount extends string = string,
  TAccountTreasury extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  destination: TransactionSigner<TAccountDestination>;
  mint: Address<TAccountMint>;
  /**
   * The owner's token account. The plan moves tokens out of it as delegate; the
   * owner never signs.
   */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  /** CHECK: destination ATA, created idempotently if needed. */
  destinationTokenAccount: Address<TAccountDestinationTokenAccount>;
  /** CHECK: treasury address. */
  treasury?: Address<TAccountTreasury>;
  /** CHECK: treasury ATA, created idempotently if needed. */
  treasuryTokenAccount: Address<TAccountTreasuryTokenAccount>;
  plan: Address<TAccountPlan>;
  coveredAsset: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRecoverInstruction<
  TAccountDestination extends string,
  TAccountMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountDestinationTokenAccount extends string,
  TAccountTreasury extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountPlan extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: RecoverInput<
    TAccountDestination,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountDestinationTokenAccount,
    TAccountTreasury,
    TAccountTreasuryTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecoverInstruction<
  TProgramAddress,
  TAccountDestination,
  TAccountMint,
  TAccountOwnerTokenAccount,
  TAccountDestinationTokenAccount,
  TAccountTreasury,
  TAccountTreasuryTokenAccount,
  TAccountPlan,
  TAccountCoveredAsset,
  TAccountTokenProgram,
  TAccountAssociatedTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    destination: { value: input.destination ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    destinationTokenAccount: { value: input.destinationTokenAccount ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    treasuryTokenAccount: { value: input.treasuryTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: true },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.treasury.value) {
    accounts.treasury.value =
      "tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q" as Address<"tr31o8FF9v2rEukh84ZwjRQgYa3x74PHssighePMP1Q">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("destinationTokenAccount", accounts.destinationTokenAccount),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("treasuryTokenAccount", accounts.treasuryTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecoverInstructionDataEncoder().encode({}),
    programAddress,
  } as RecoverInstruction<
    TProgramAddress,
    TAccountDestination,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountDestinationTokenAccount,
    TAccountTreasury,
    TAccountTreasuryTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedRecoverInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    destination: TAccountMetas[0];
    mint: TAccountMetas[1];
    /**
     * The owner's token account. The plan moves tokens out of it as delegate; the
     * owner never signs.
     */
    ownerTokenAccount: TAccountMetas[2];
    /** CHECK: destination ATA, created idempotently if needed. */
    destinationTokenAccount: TAccountMetas[3];
    /** CHECK: treasury address. */
    treasury: TAccountMetas[4];
    /** CHECK: treasury ATA, created idempotently if needed. */
    treasuryTokenAccount: TAccountMetas[5];
    plan: TAccountMetas[6];
    coveredAsset: TAccountMetas[7];
    /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
    tokenProgram: TAccountMetas[8];
    associatedTokenProgram: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
  };
  data: RecoverInstructionData;
};

export function parseRecoverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecoverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 11,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      destination: getNextAccount(),
      mint: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      destinationTokenAccount: getNextAccount(),
      treasury: getNextAccount(),
      treasuryTokenAccount: getNextAccount(),
      plan: getNextAccount(),
      coveredAsset: getNextAccount(),
      tokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecoverInstructionDataDecoder().decode(instruction.data),
  };
}
