/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const VAULT_DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  231, 150, 41, 113, 180, 104, 162, 120,
]);

export function getVaultDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DEPOSIT_DISCRIMINATOR);
}

export type VaultDepositInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountCoveredAsset extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountPlan extends string ? ReadonlyAccount<TAccountPlan> : TAccountPlan,
      TAccountCoveredAsset extends string
        ? ReadonlyAccount<TAccountCoveredAsset>
        : TAccountCoveredAsset,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type VaultDepositInstructionData = { discriminator: ReadonlyUint8Array; amount: bigint };

export type VaultDepositInstructionDataArgs = { amount: number | bigint };

export function getVaultDepositInstructionDataEncoder(): FixedSizeEncoder<VaultDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VAULT_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getVaultDepositInstructionDataDecoder(): FixedSizeDecoder<VaultDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getVaultDepositInstructionDataCodec(): FixedSizeCodec<
  VaultDepositInstructionDataArgs,
  VaultDepositInstructionData
> {
  return combineCodec(
    getVaultDepositInstructionDataEncoder(),
    getVaultDepositInstructionDataDecoder(),
  );
}

export type VaultDepositInput<
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountPlan extends string = string,
  TAccountCoveredAsset extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  plan: Address<TAccountPlan>;
  coveredAsset: Address<TAccountCoveredAsset>;
  /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: VaultDepositInstructionDataArgs["amount"];
};

export function getVaultDepositInstruction<
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountVaultTokenAccount extends string,
  TAccountPlan extends string,
  TAccountCoveredAsset extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: VaultDepositInput<
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): VaultDepositInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountMint,
  TAccountOwnerTokenAccount,
  TAccountVaultTokenAccount,
  TAccountPlan,
  TAccountCoveredAsset,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    ownerTokenAccount: { value: input.ownerTokenAccount ?? null, isWritable: true },
    vaultTokenAccount: { value: input.vaultTokenAccount ?? null, isWritable: true },
    plan: { value: input.plan ?? null, isWritable: false },
    coveredAsset: { value: input.coveredAsset ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("coveredAsset", accounts.coveredAsset),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getVaultDepositInstructionDataEncoder().encode(args as VaultDepositInstructionDataArgs),
    programAddress,
  } as VaultDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountPlan,
    TAccountCoveredAsset,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedVaultDepositInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    mint: TAccountMetas[1];
    ownerTokenAccount: TAccountMetas[2];
    vaultTokenAccount: TAccountMetas[3];
    plan: TAccountMetas[4];
    coveredAsset: TAccountMetas[5];
    /** CHECK: verified below via constraint, Switch to Interface<TokenInterface>/similar on stable release. */
    tokenProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: VaultDepositInstructionData;
};

export function parseVaultDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 8,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      mint: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      plan: getNextAccount(),
      coveredAsset: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getVaultDepositInstructionDataDecoder().decode(instruction.data),
  };
}
