/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findInitializeVaultPlanPlanPda } from "../pdas";
import { HEIRLOOM_STOCKS_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_VAULT_PLAN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  166, 73, 6, 83, 90, 65, 70, 158,
]);

export function getInitializeVaultPlanDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_VAULT_PLAN_DISCRIMINATOR);
}

export type InitializeVaultPlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountCheckinSigner extends string | AccountMeta<string> = string,
  TAccountGuardian extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountDestination extends string
        ? ReadonlyAccount<TAccountDestination>
        : TAccountDestination,
      TAccountCheckinSigner extends string
        ? ReadonlyAccount<TAccountCheckinSigner>
        : TAccountCheckinSigner,
      TAccountGuardian extends string ? ReadonlyAccount<TAccountGuardian> : TAccountGuardian,
      TAccountPlan extends string ? WritableAccount<TAccountPlan> : TAccountPlan,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeVaultPlanInstructionData = {
  discriminator: ReadonlyUint8Array;
  checkinIntervalSecs: bigint;
  gracePeriodSecs: bigint;
  pauseDurationSecs: bigint;
};

export type InitializeVaultPlanInstructionDataArgs = {
  checkinIntervalSecs: number | bigint;
  gracePeriodSecs: number | bigint;
  pauseDurationSecs: number | bigint;
};

export function getInitializeVaultPlanInstructionDataEncoder(): FixedSizeEncoder<InitializeVaultPlanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["checkinIntervalSecs", getI64Encoder()],
      ["gracePeriodSecs", getI64Encoder()],
      ["pauseDurationSecs", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_VAULT_PLAN_DISCRIMINATOR }),
  );
}

export function getInitializeVaultPlanInstructionDataDecoder(): FixedSizeDecoder<InitializeVaultPlanInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["checkinIntervalSecs", getI64Decoder()],
    ["gracePeriodSecs", getI64Decoder()],
    ["pauseDurationSecs", getI64Decoder()],
  ]);
}

export function getInitializeVaultPlanInstructionDataCodec(): FixedSizeCodec<
  InitializeVaultPlanInstructionDataArgs,
  InitializeVaultPlanInstructionData
> {
  return combineCodec(
    getInitializeVaultPlanInstructionDataEncoder(),
    getInitializeVaultPlanInstructionDataDecoder(),
  );
}

export type InitializeVaultPlanAsyncInput<
  TAccountOwner extends string = string,
  TAccountDestination extends string = string,
  TAccountCheckinSigner extends string = string,
  TAccountGuardian extends string = string,
  TAccountPlan extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** CHECK: heir pubkey, stored on the plan. */
  destination: Address<TAccountDestination>;
  /** CHECK: optional hot wallet allowed to check in only. */
  checkinSigner?: Address<TAccountCheckinSigner>;
  /** CHECK: optional guardian allowed to defer only. */
  guardian?: Address<TAccountGuardian>;
  plan?: Address<TAccountPlan>;
  systemProgram?: Address<TAccountSystemProgram>;
  checkinIntervalSecs: InitializeVaultPlanInstructionDataArgs["checkinIntervalSecs"];
  gracePeriodSecs: InitializeVaultPlanInstructionDataArgs["gracePeriodSecs"];
  pauseDurationSecs: InitializeVaultPlanInstructionDataArgs["pauseDurationSecs"];
};

export async function getInitializeVaultPlanInstructionAsync<
  TAccountOwner extends string,
  TAccountDestination extends string,
  TAccountCheckinSigner extends string,
  TAccountGuardian extends string,
  TAccountPlan extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: InitializeVaultPlanAsyncInput<
    TAccountOwner,
    TAccountDestination,
    TAccountCheckinSigner,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeVaultPlanInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountDestination,
    TAccountCheckinSigner,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: false },
    checkinSigner: { value: input.checkinSigner ?? null, isWritable: false },
    guardian: { value: input.guardian ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.plan.value) {
    accounts.plan.value = await findInitializeVaultPlanPlanPda({
      owner: getAddressFromResolvedInstructionAccount("owner", accounts.owner.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("checkinSigner", accounts.checkinSigner),
      getAccountMeta("guardian", accounts.guardian),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeVaultPlanInstructionDataEncoder().encode(
      args as InitializeVaultPlanInstructionDataArgs,
    ),
    programAddress,
  } as InitializeVaultPlanInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountDestination,
    TAccountCheckinSigner,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >);
}

export type InitializeVaultPlanInput<
  TAccountOwner extends string = string,
  TAccountDestination extends string = string,
  TAccountCheckinSigner extends string = string,
  TAccountGuardian extends string = string,
  TAccountPlan extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** CHECK: heir pubkey, stored on the plan. */
  destination: Address<TAccountDestination>;
  /** CHECK: optional hot wallet allowed to check in only. */
  checkinSigner?: Address<TAccountCheckinSigner>;
  /** CHECK: optional guardian allowed to defer only. */
  guardian?: Address<TAccountGuardian>;
  plan: Address<TAccountPlan>;
  systemProgram?: Address<TAccountSystemProgram>;
  checkinIntervalSecs: InitializeVaultPlanInstructionDataArgs["checkinIntervalSecs"];
  gracePeriodSecs: InitializeVaultPlanInstructionDataArgs["gracePeriodSecs"];
  pauseDurationSecs: InitializeVaultPlanInstructionDataArgs["pauseDurationSecs"];
};

export function getInitializeVaultPlanInstruction<
  TAccountOwner extends string,
  TAccountDestination extends string,
  TAccountCheckinSigner extends string,
  TAccountGuardian extends string,
  TAccountPlan extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
>(
  input: InitializeVaultPlanInput<
    TAccountOwner,
    TAccountDestination,
    TAccountCheckinSigner,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeVaultPlanInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountDestination,
  TAccountCheckinSigner,
  TAccountGuardian,
  TAccountPlan,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_STOCKS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: false },
    checkinSigner: { value: input.checkinSigner ?? null, isWritable: false },
    guardian: { value: input.guardian ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("checkinSigner", accounts.checkinSigner),
      getAccountMeta("guardian", accounts.guardian),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeVaultPlanInstructionDataEncoder().encode(
      args as InitializeVaultPlanInstructionDataArgs,
    ),
    programAddress,
  } as InitializeVaultPlanInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountDestination,
    TAccountCheckinSigner,
    TAccountGuardian,
    TAccountPlan,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeVaultPlanInstruction<
  TProgram extends string = typeof HEIRLOOM_STOCKS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    /** CHECK: heir pubkey, stored on the plan. */
    destination: TAccountMetas[1];
    /** CHECK: optional hot wallet allowed to check in only. */
    checkinSigner?: TAccountMetas[2] | undefined;
    /** CHECK: optional guardian allowed to defer only. */
    guardian?: TAccountMetas[3] | undefined;
    plan: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: InitializeVaultPlanInstructionData;
};

export function parseInitializeVaultPlanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeVaultPlanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === HEIRLOOM_STOCKS_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      destination: getNextAccount(),
      checkinSigner: getNextOptionalAccount(),
      guardian: getNextOptionalAccount(),
      plan: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeVaultPlanInstructionDataDecoder().decode(instruction.data),
  };
}
