/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findEstatePda, findVaultPda } from "../pdas";
import { HEIRLOOM_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ESTATE_DISCRIMINATOR = new Uint8Array([8]);

export function getCloseEstateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    CLOSE_ESTATE_DISCRIMINATOR,
  );
}

export type CloseEstateInstruction<
  TProgram extends string = typeof HEIRLOOM_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountHeir extends string | AccountMeta<string> = string,
  TAccountEstate extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountHeir extends string
        ? ReadonlyAccount<TAccountHeir>
        : TAccountHeir,
      TAccountEstate extends string
        ? WritableAccount<TAccountEstate>
        : TAccountEstate,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseEstateInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseEstateInstructionDataArgs = {};

export function getCloseEstateInstructionDataEncoder(): FixedSizeEncoder<CloseEstateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: CLOSE_ESTATE_DISCRIMINATOR }),
  );
}

export function getCloseEstateInstructionDataDecoder(): FixedSizeDecoder<CloseEstateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getCloseEstateInstructionDataCodec(): FixedSizeCodec<
  CloseEstateInstructionDataArgs,
  CloseEstateInstructionData
> {
  return combineCodec(
    getCloseEstateInstructionDataEncoder(),
    getCloseEstateInstructionDataDecoder(),
  );
}

export type CloseEstateAsyncInput<
  TAccountAuthority extends string = string,
  TAccountHeir extends string = string,
  TAccountEstate extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  heir: Address<TAccountHeir>;
  estate?: Address<TAccountEstate>;
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCloseEstateInstructionAsync<
  TAccountAuthority extends string,
  TAccountHeir extends string,
  TAccountEstate extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseEstateAsyncInput<
    TAccountAuthority,
    TAccountHeir,
    TAccountEstate,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseEstateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHeir,
    TAccountEstate,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HEIRLOOM_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    heir: { value: input.heir ?? null, isWritable: false },
    estate: { value: input.estate ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.estate.value) {
    accounts.estate.value = await findEstatePda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
      heir: getAddressFromResolvedInstructionAccount(
        "heir",
        accounts.heir.value,
      ),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
      heir: getAddressFromResolvedInstructionAccount(
        "heir",
        accounts.heir.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("heir", accounts.heir),
      getAccountMeta("estate", accounts.estate),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseEstateInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseEstateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHeir,
    TAccountEstate,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type CloseEstateInput<
  TAccountAuthority extends string = string,
  TAccountHeir extends string = string,
  TAccountEstate extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  heir: Address<TAccountHeir>;
  estate: Address<TAccountEstate>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseEstateInstruction<
  TAccountAuthority extends string,
  TAccountHeir extends string,
  TAccountEstate extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseEstateInput<
    TAccountAuthority,
    TAccountHeir,
    TAccountEstate,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseEstateInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountHeir,
  TAccountEstate,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? HEIRLOOM_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    heir: { value: input.heir ?? null, isWritable: false },
    estate: { value: input.estate ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("heir", accounts.heir),
      getAccountMeta("estate", accounts.estate),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseEstateInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseEstateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHeir,
    TAccountEstate,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedCloseEstateInstruction<
  TProgram extends string = typeof HEIRLOOM_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    heir: TAccountMetas[1];
    estate: TAccountMetas[2];
    vault: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CloseEstateInstructionData;
};

export function parseCloseEstateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseEstateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      heir: getNextAccount(),
      estate: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseEstateInstructionDataDecoder().decode(instruction.data),
  };
}
