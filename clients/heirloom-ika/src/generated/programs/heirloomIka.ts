/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import { getEstateCodec, type Estate, type EstateArgs } from "../accounts";
import {
  getClaimInstruction,
  getHeartbeatInstruction,
  getInitializeInstructionAsync,
  getRevokeInstruction,
  parseClaimInstruction,
  parseHeartbeatInstruction,
  parseInitializeInstruction,
  parseRevokeInstruction,
  type ClaimInput,
  type HeartbeatInput,
  type InitializeAsyncInput,
  type ParsedClaimInstruction,
  type ParsedHeartbeatInstruction,
  type ParsedInitializeInstruction,
  type ParsedRevokeInstruction,
  type RevokeInput,
} from "../instructions";
import { findEstatePda } from "../pdas";

export const HEIRLOOM_IKA_PROGRAM_ADDRESS =
  "9ede3aHXJiv14BNT67MWpgFGugtP1PSdBuLDuRX2D4sf" as Address<"9ede3aHXJiv14BNT67MWpgFGugtP1PSdBuLDuRX2D4sf">;

export enum HeirloomIkaAccount {
  Estate,
}

export function identifyHeirloomIkaAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HeirloomIkaAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([193, 23, 206, 61, 104, 225, 211, 221]),
      ),
      0,
    )
  ) {
    return HeirloomIkaAccount.Estate;
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT, {
    accountData: data,
    programName: "heirloomIka",
  });
}

export enum HeirloomIkaInstruction {
  Claim,
  Heartbeat,
  Initialize,
  Revoke,
}

export function identifyHeirloomIkaInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HeirloomIkaInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([62, 198, 214, 193, 213, 159, 108, 210]),
      ),
      0,
    )
  ) {
    return HeirloomIkaInstruction.Claim;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([202, 104, 56, 6, 240, 170, 63, 134]),
      ),
      0,
    )
  ) {
    return HeirloomIkaInstruction.Heartbeat;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]),
      ),
      0,
    )
  ) {
    return HeirloomIkaInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([170, 23, 31, 34, 133, 173, 93, 242]),
      ),
      0,
    )
  ) {
    return HeirloomIkaInstruction.Revoke;
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION, {
    instructionData: data,
    programName: "heirloomIka",
  });
}

export type ParsedHeirloomIkaInstruction<
  TProgram extends string = "9ede3aHXJiv14BNT67MWpgFGugtP1PSdBuLDuRX2D4sf",
> =
  | ({ instructionType: HeirloomIkaInstruction.Claim } & ParsedClaimInstruction<TProgram>)
  | ({ instructionType: HeirloomIkaInstruction.Heartbeat } & ParsedHeartbeatInstruction<TProgram>)
  | ({ instructionType: HeirloomIkaInstruction.Initialize } & ParsedInitializeInstruction<TProgram>)
  | ({ instructionType: HeirloomIkaInstruction.Revoke } & ParsedRevokeInstruction<TProgram>);

export function parseHeirloomIkaInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedHeirloomIkaInstruction<TProgram> {
  const instructionType = identifyHeirloomIkaInstruction(instruction);
  switch (instructionType) {
    case HeirloomIkaInstruction.Claim: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HeirloomIkaInstruction.Claim,
        ...parseClaimInstruction(instruction),
      };
    }
    case HeirloomIkaInstruction.Heartbeat: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HeirloomIkaInstruction.Heartbeat,
        ...parseHeartbeatInstruction(instruction),
      };
    }
    case HeirloomIkaInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HeirloomIkaInstruction.Initialize,
        ...parseInitializeInstruction(instruction),
      };
    }
    case HeirloomIkaInstruction.Revoke: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HeirloomIkaInstruction.Revoke,
        ...parseRevokeInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE, {
        instructionType: instructionType as string,
        programName: "heirloomIka",
      });
  }
}

export type HeirloomIkaPlugin = {
  accounts: HeirloomIkaPluginAccounts;
  instructions: HeirloomIkaPluginInstructions;
  pdas: HeirloomIkaPluginPdas;
};

export type HeirloomIkaPluginAccounts = {
  estate: ReturnType<typeof getEstateCodec> & SelfFetchFunctions<EstateArgs, Estate>;
};

export type HeirloomIkaPluginInstructions = {
  claim: (input: ClaimInput) => ReturnType<typeof getClaimInstruction> & SelfPlanAndSendFunctions;
  heartbeat: (
    input: HeartbeatInput,
  ) => ReturnType<typeof getHeartbeatInstruction> & SelfPlanAndSendFunctions;
  initialize: (
    input: InitializeAsyncInput,
  ) => ReturnType<typeof getInitializeInstructionAsync> & SelfPlanAndSendFunctions;
  revoke: (
    input: RevokeInput,
  ) => ReturnType<typeof getRevokeInstruction> & SelfPlanAndSendFunctions;
};

export type HeirloomIkaPluginPdas = { estate: typeof findEstatePda };

export type HeirloomIkaPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function heirloomIkaProgram() {
  return <T extends HeirloomIkaPluginRequirements>(client: T) => {
    return {
      ...client,
      heirloomIka: <HeirloomIkaPlugin>{
        accounts: { estate: addSelfFetchFunctions(client, getEstateCodec()) },
        instructions: {
          claim: (input) => addSelfPlanAndSendFunctions(client, getClaimInstruction(input)),
          heartbeat: (input) => addSelfPlanAndSendFunctions(client, getHeartbeatInstruction(input)),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(client, getInitializeInstructionAsync(input)),
          revoke: (input) => addSelfPlanAndSendFunctions(client, getRevokeInstruction(input)),
        },
        pdas: { estate: findEstatePda },
      },
    };
  };
}
