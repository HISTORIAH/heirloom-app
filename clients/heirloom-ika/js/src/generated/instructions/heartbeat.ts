/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { HEIRLOOM_IKA_PROGRAM_ADDRESS } from "../programs";

export const HEARTBEAT_DISCRIMINATOR = new Uint8Array([202, 104, 56, 6, 240, 170, 63, 134]);

export function getHeartbeatDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HEARTBEAT_DISCRIMINATOR);
}

export type HeartbeatInstruction<
  TProgram extends string = typeof HEIRLOOM_IKA_PROGRAM_ADDRESS,
  TAccountRelayer extends string | AccountMeta<string> = string,
  TAccountEstate extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRelayer extends string
        ? WritableSignerAccount<TAccountRelayer> & AccountSignerMeta<TAccountRelayer>
        : TAccountRelayer,
      TAccountEstate extends string ? WritableAccount<TAccountEstate> : TAccountEstate,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      ...TRemainingAccounts,
    ]
  >;

export type HeartbeatInstructionData = { discriminator: ReadonlyUint8Array };

export type HeartbeatInstructionDataArgs = {};

export function getHeartbeatInstructionDataEncoder(): FixedSizeEncoder<HeartbeatInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: HEARTBEAT_DISCRIMINATOR }),
  );
}

export function getHeartbeatInstructionDataDecoder(): FixedSizeDecoder<HeartbeatInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getHeartbeatInstructionDataCodec(): FixedSizeCodec<
  HeartbeatInstructionDataArgs,
  HeartbeatInstructionData
> {
  return combineCodec(getHeartbeatInstructionDataEncoder(), getHeartbeatInstructionDataDecoder());
}

export type HeartbeatInput<
  TAccountRelayer extends string = string,
  TAccountEstate extends string = string,
  TAccountInstructions extends string = string,
> = {
  /** Relayer pays gas — has no authority over the estate */
  relayer: TransactionSigner<TAccountRelayer>;
  estate: Address<TAccountEstate>;
  instructions?: Address<TAccountInstructions>;
};

export function getHeartbeatInstruction<
  TAccountRelayer extends string,
  TAccountEstate extends string,
  TAccountInstructions extends string,
  TProgramAddress extends Address = typeof HEIRLOOM_IKA_PROGRAM_ADDRESS,
>(
  input: HeartbeatInput<TAccountRelayer, TAccountEstate, TAccountInstructions>,
  config?: { programAddress?: TProgramAddress },
): HeartbeatInstruction<TProgramAddress, TAccountRelayer, TAccountEstate, TAccountInstructions> {
  // Program address.
  const programAddress = config?.programAddress ?? HEIRLOOM_IKA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    relayer: { value: input.relayer ?? null, isWritable: true },
    estate: { value: input.estate ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("relayer", accounts.relayer),
      getAccountMeta("estate", accounts.estate),
      getAccountMeta("instructions", accounts.instructions),
    ],
    data: getHeartbeatInstructionDataEncoder().encode({}),
    programAddress,
  } as HeartbeatInstruction<
    TProgramAddress,
    TAccountRelayer,
    TAccountEstate,
    TAccountInstructions
  >);
}

export type ParsedHeartbeatInstruction<
  TProgram extends string = typeof HEIRLOOM_IKA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Relayer pays gas — has no authority over the estate */
    relayer: TAccountMetas[0];
    estate: TAccountMetas[1];
    instructions: TAccountMetas[2];
  };
  data: HeartbeatInstructionData;
};

export function parseHeartbeatInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedHeartbeatInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      relayer: getNextAccount(),
      estate: getNextAccount(),
      instructions: getNextAccount(),
    },
    data: getHeartbeatInstructionDataDecoder().decode(instruction.data),
  };
}
