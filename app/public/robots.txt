# https://app.heirlm.xyz — the application. Every route is wallet-gated and
# per-user, so there is nothing here to index. The public, indexable site is
# https://heirlm.xyz, which serves its own robots.txt and sitemap.
User-agent: *
Disallow: /
